declare fn putchar(ch: int)
declare fn printf(pattern: *u8)

fn main() -> int {
  let a = 130;
  let b = 20;

  if a >= b {
    hello();
  }
  return 0;
}

fn greater_than_or_equal_to_ten(a: int) -> bool {
  return a >= 10;
}

fn hello() {
  printf("hello from function hello");
} 