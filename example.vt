declare fn printf(d: int) -> i64

fn fibonacci(l: int, r: int, n: int) -> int {
    if (n == 0) { return l; }
    return fibonacci(r, l + r, n-1);
}

fn main() -> int {
    return fibonacci(0, 1, 10);
}
